{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }}
	-
{% endblock %}

{% block body %}
<pre>
	{{ dump(article) }}
</pre>
	<div class="card w-75">
		{# <div class="card-body">
			<h5 class="card-title">{{ article[0][0].title }}</h5>
			<h6 class="card-title">{{ article[0][0].date.date|date('d/m/Y') }}</h6>
			<p class="card-text">
				{{ article[0][0].author }}
			</p>
			<a href=" " class="btn btn-primary">Article</a>
		</div> #}
	</div>
{% endblock %}
